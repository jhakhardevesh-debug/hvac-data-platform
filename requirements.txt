# AWS — connects Python to Amazon S3
boto3==1.34.0

# Snowflake — connects Python to Snowflake database
snowflake-connector-python==3.6.0

# Airflow — pipeline orchestration and scheduling
apache-airflow==2.8.1

# Airflow Snowflake provider — lets Airflow talk to Snowflake
apache-airflow-providers-snowflake==5.1.0

# dbt core engine
dbt-core==1.7.0

# dbt Snowflake adapter — lets dbt talk to Snowflake
dbt-snowflake==1.7.0

# Data manipulation
pandas==2.1.4

# Reads .env file into environment variables
python-dotenv==1.0.0

# Generates realistic fake data for simulator
faker==22.2.0

# Testing framework
pytest==7.4.4

# Code formatter — enforces consistent style
black==23.12.1